@model IEnumerable<SaleViewModel>

@{
    ViewData["Title"] = "SaleTable";
}

<h1>SaleTable</h1>

<table class="table table-bordered table-sm table-striped">
    <thead>
        <tr class="text-center">
            <th>
                @Html.DisplayNameFor(model => model.ProductName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateOfSale)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr class="text-center">
            <td>
                @Html.DisplayFor(modelItem => item.ProductName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateOfSale)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                <button asp-action="SaleUpdating" class="btn btn-sm btn-outline-primary">@Html.ActionLink("Edit", "SaleUpdating", new { id = item.SaleId })</button>
                <button class="btn btn-sm btn-outline-primary" onclick="return confirm('Are you shure you want to delete product : @item.SaleId ')">@Html.ActionLink("Delete", "SaleDeleting", new { id = item.SaleId })</button>
            </td>
        </tr>
}
    </tbody>
</table>

<p align="right">
    <a asp-action="SaleCreating" class="btn btn-sm btn-secondary">Create New</a>
</p>
